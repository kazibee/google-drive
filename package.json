{
  "name": "@kazibee/google-drive",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "command": "./src/command.ts",
  "dependencies": {
    "@googleapis/drive": "^8.0.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8"
  },
  "kazibee": {
    "description": "Manage Google Drive files and folders with OAuth2 authentication",
    "commands": {
      "login": { "description": "Run the OAuth2 browser login flow to obtain and store Google credentials" }
    }
  }
}
